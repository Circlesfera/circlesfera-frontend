import { type ReactElement } from 'react';
import { ReelsShell } from '@/modules/reels/components/reels-shell';

export const metadata = {
  title: 'Reels — CircleSfera'
};

export default function ReelsPage(): ReactElement {
  return <ReelsShell />;
}

